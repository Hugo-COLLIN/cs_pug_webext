extends layout

block content
  h1= title
  .posts
    each post in posts
      article.post-preview
        h2: a(href=`/blog/${post.slug}.html`)= post.title
        time= post.date
        p= post.excerpt
        if post.tags && post.tags.length
          .tags
            each tag in post.tags
              span.tag= tag
