extends layout

block content
  h1= title
  .content
    p= content

  section.recent-posts
    h2 Recent Blog Posts
    if posts
      ul
        each post in posts.slice(0, 3)
          li
            h3: a(href=`/blog/${post.slug}.html`)= post.title
            p= post.excerpt
            small= post.date
